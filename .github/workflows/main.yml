name: first-time-trying-git-actions

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: "Testing Github action"
        run: pwd
      
      - name: "Testing Printing"
        run: echo AHead
